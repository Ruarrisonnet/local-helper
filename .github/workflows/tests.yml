name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python: ["3.13"]
        include:
          - os: ubuntu-22.04
            python: "3.8"
          - os: windows-latest
            python: "3.8"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Compile
        run: python -m py_compile server.py outline.py enforce.py install.py backend.py search.py bench.py bench_fixture.py mock_backend.py
      - name: Unit tests
        run: python test_units.py
      - name: Model-path tests (mock Ollama and OpenAI-compatible servers)
        run: python test_models.py
      - name: Hook tests
        run: python test_enforce.py
      - name: Server tests (no Ollama on CI, so model checks are reported as SKIP)
        run: python test_server.py
      - name: Benchmark parser and graders (spends nothing, starts no session)
        run: python bench.py --self-check
      - name: Benchmark fixtures (truth is right, and grep gets it wrong)
        run: python bench_fixture.py
